/// <reference types="vite/client" />

interface ImportMetaEnv {
  // 다른 환경 변수들에 대한 타입 정의...
  readonly VITE_POKE_API_URL: string;
  readonly VITE_POKE_IMG_BASE_URL: string;
  readonly VITE_POKE_SPECIES_URL: stirng;
}
